spring.datasource.url: jdbc:h2:file:./camunda-h2-database

camunda.bpm:
  admin-user:
    id: demo
    password: demo
  filter.create: All Tasks

kafka:
  bootstrap-address: localhost:9092
  group-id: someGroupId
  trusted-packages: ch.unisg.ems.sales.dto

server:
  port: 8083